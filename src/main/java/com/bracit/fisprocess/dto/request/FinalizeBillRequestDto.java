package com.bracit.fisprocess.dto.request;

import lombok.Data;

/**
 * Request DTO for finalizing an AP Bill (action trigger, no body required).
 */
@Data
public class FinalizeBillRequestDto {
    // Action trigger — no fields needed
}
